import config from '@config'

// pedir más poes si debo
export const getIfShould = ({ commit, state }) => {
  // aquí pediremos si estamos en el primer poe (posición 0),
  // o si estamos en el último o penúltimo poe
}

export const getPoes = ({ commit, state }) => {
  // aquí pediremos si estamos en el primer poe (posición 0),
  // o si estamos en el último o penúltimo poe
}
